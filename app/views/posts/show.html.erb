<article>
    <h1><%= @post.title %></h1>
    <section class="timeInfo">
        <small>Posted At: <%= @post.created_at.strftime("%Y-%m-%d %H:%M") %> / Updated At: <%= @post.updated_at.strftime("%Y-%m-%d %H:%M") %></small>
    </section>
    <p><%= markdown @post.content %></p>
</article>